# Spins up a throw-away Postgres, migrates with Flyway, then generates
# jOOQ classes into target/generated-sources/jooq on the host.
#
# Usage:  task generate
services:

  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: javersdemo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d javersdemo"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  codegen:
    image: maven:3.9.13-eclipse-temurin-25-alpine
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - .:/project
      - maven-cache:/root/.m2
    working_dir: /project
    command: >
      mvn -Pjooq generate-sources
        -Dcodegen.db.url=jdbc:postgresql://postgres:5432/javersdemo
        -Dcodegen.db.user=postgres
        -Dcodegen.db.password=password

volumes:
  maven-cache:
